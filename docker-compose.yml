version: '3.8'
services:
  api:
    image: sentinelnet-api
    ports:
      - 8080:8080
    volumes:
      - data:/usr/src/app/data
  sensor:
    image: sentinelnet-sensor
    deploy:
      replicas: 3
      mode: global
volumes:
  data:
